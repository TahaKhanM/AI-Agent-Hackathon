# Crib — T3 · the conformance bench · independent oracle · 6/6 · extractor robustness · 25k realism

*Pocket card. One page. You are the numbers owner — you carry the source table. Everything you say traces to `Prep/final-numbers.md`. If a number isn't on that card, don't say it.*

## Your job on stage
- **Numbers owner.** Every quantitative claim and its source runs through you; you own the conformance bench, the independent oracle, the 6/6 attacks, extractor robustness, and 25k-record realism.
- First-touch on any challenged number: give the digit + source + caveat, then hand narrative back to T1 or tech depth to T2.
- If a challenged number isn't on your card: **"Good challenge — it's in our sourced appendix; the discipline matters more than the digit."** Then hand to T1.

## Your 5 highest-value answers (bold opener verbatim, then the follow-through)

**1. "How do you know your bench isn't self-grading?"**
> **"Ground truth comes from an independent oracle — a second, AST-guarded implementation, never the compiler grading itself."**
The conformance bench (`bench/oracle.py`) is a two-implementation cross-check over the topology: 5 hierarchy levels · 20 roles · 1,000 ACL-tagged docs · 40 principals · 10,000 queries (5,219 deny-expected, 4,781 allow-expected). Result: **0 leaks / 5,219 = 0.000% FNR** and **0 / 4,781 = 0.000% FPR**, both far under threshold.

**2. "What adversarial cases did you actually test?"**
> **"Six named attacks, and we resist 6/6."**
Verbatim from `tests/test_adversarial.py`: **query inference · metadata bypass · timing attack · collection attack · prompt injection · derived-memory attack.** All six pass at seed 4207; the correctness fields are a byte-identical function of the seed.

**3. "Your 94% match key includes the resolution code you only know after the fix — what can triage match at arrival?"**
> **"We measured exactly that — three honest numbers, and they don't get conflated."**
94.4% measures fix-class *existence* (the key includes `closed_code`, known only at resolution). At arrival, symptom-level: **98.6% precedent existence**; a naive frequency baseline ranks the true fix **top-1 59.4% / top-3 87.7%** over 24,470 evaluable incidents — a **naive FLOOR**, not product accuracy. The 59% top-1 is exactly *why* execution requires extractor-confirmed fingerprint equality, never rank-1 similarity.

**4. "Can a messy / garbled ticket fast-path a wrong fix?"**
> **"Zero false-fast-paths out of 100 mutations — that's the eligibility-relevant invariant, and it's 0.00%."**
On an adversarial corpus (92% codeless/garbled/unknown-sibling): correct-match 8 · safe-degrade 50 · conservative-degrade 42 · **false-fast-path 0**. And **25/25 red-herring decoys resisted (100%)** — an unknown code degrades to human review rather than grabbing a look-alike known code. Recoverable known-code tickets: 8/8 correct.

**5. "Your KB has 10 articles — a real one has 50k. Show me the number at scale."**
> **"Over 24,918 real incidents, 94.4% arrived with their exact fix class already precedented — and those repeats still took a median 18.2 calendar hours to resolve by hand."**
Retrieval, not resolution, is the bottleneck. The store is the UCI ServiceNow-derived log (CC BY 4.0) with its nulls and dup titles kept — realistic by design. The permission curve stays flat / O(1) from 1k to 100k because the check is one indexed lookup.

## The numbers you own (from `final-numbers.md`, with labels — seed 4207)
- **94.4% = fix-class EXISTENCE** (closed_code known at resolution; 1,397 classes). **98.6% = arrival-knowable symptom class** (335 classes). Don't swap.
- **top-1 59.4% / top-3 87.7% = naive FLOOR** (24,470 evaluable), not product accuracy.
- **Median precedented repeat 18.2h CALENDAR** (p75 136.6h; first-of-class median 92.1h). Baseline bar anchor = **8.85 business hours = "8h 51m"** (MetricNet, industry, BUSINESS).
- **Store = 25k records (24,918 incidents)**; provenance line only: 141,712 events.
- Bench: **FNR 0/5,219 = 0.000% · FPR 0/4,781 = 0.000% · derived 3,000/3,000 = 100% · audit 300/300 = 100% · attacks 6/6.**
- Extractor: **0 false-fast-paths / 100 (0.00%) · 25/25 decoys resisted.**
- Data-quality colour: 99.55% complete fingerprint · 0.43% missing closed_code · 6.2% missing resolved_at · 47.4% of repeats reassigned ≥1×.

## Your two takeaway lines (land these no matter what)
1. **"Zero false-fast-paths out of a hundred — the messy ticket never grabs the wrong fix."**
2. **"Ground truth is an independent oracle — we never grade ourselves."**

## Do NOT say (honesty traps for your area)
- **Swapping 94.4% (existence) with 98.6% (arrival-knowable)**, or calling 59.4%/87.7% "our accuracy" — it's a **naive floor**.
- **Blending 18.2h (calendar, ours/UCI) with 8h 51m / 8.85h (business, MetricNet).** Never averaged or swapped.
- **"P99 over 141k events."** P99 is over the **25k-record store**; 141k is a provenance line only.
- Presenting the µs/ms latency figures as fixed — they drift run-to-run; quote the committed values (~5 orders under threshold).
- The `knowledge=true` 74.6h vs 8.6h figures as causal ("documented fix → faster") — colour only; acknowledge the confound if probed.
- Any vendor number without **"(vendor-claimed)"** (or NeuBird's **"(2026 vendor-sponsored survey)"**).
- **"$400B combined"** (use $200M/company) or reintroducing **TMDB/IMDb** (rejected on licence grounds).
- **"Autonomous" for L3** — it's **"Standing Approval — a pre-approved standard change."**
- Any closed model name; open-weight only, roles are FAST/SMART/HEAVY/EMBED, ids live only in `precedent/models.py`.

ultracode

# Mission: take Precedent from panel-refined to submission-winning

You are working in the Precedent hackathon repo (`/Users/tahakhan/Documents/Work/Projects/AI-Agent-Hackathon`). Precedent is our entry for the UK AI Agent Hackathon EP5 × Conduct: an agent that detects enterprise incidents, retrieves the documented admin/config fix from the organisation's own knowledge, and executes it with approval gates, audit trails and rollback — "AI SREs fix broken code; in real enterprises the fix is almost never code."

Your mission: **refine, build out and develop this idea to its strongest possible form for the three tracks we are entering**, using a multi-agent system of roleplayed judges, builder agents and adversarial verifiers. The idea has already survived one simulated judging round (scores went Conduct 73→79, Fetch 68→80, BasedAI 58→80, Technical 59→80, VC 77→79); your job is to close the remaining gap to a winning entry. Treat every weakness you leave unfixed as a question we will be asked in judging.

I'm telling you the intent, not the steps: you decide the exact agent topology, round structure and convergence tactics. The requirements below are the contract, not the method.

## Read this context first (in this order)

1. `Idea/Idea-Development.md` — the current idea (v4, panel-refined). This is the document you will evolve.
2. `Idea/refinement/00-panel-verdicts.md` — the previous panel's scores, fixed items, and the **remaining fatal/major items** (all still authoritative unless you verify otherwise); then the five deliverable specs `refinement/01`–`05` (data plan, architecture, deck, demo/video scripts, scale story & Q&A).
3. `CLAUDE-AVAILABLE-APIS.md` — current provisioning state. Read it precisely, not optimistically: Jira JSM site live with project MEDIA, **verified for auth and issue creation only** (the JSM tier and the permission-scheme/role-flip APIs the ACL story depends on are NOT yet confirmed); Venice API access verified (GET /models returned 200 with 89 models) with text models **provisionally** pinned (FAST=`qwen3-5-35b-a3b`, SMART=`deepseek-v4-flash`, HEAVY=`deepseek-v4-pro`) — those IDs came from a third-party catalog, and the BasedAI-fatal verification (each pinned ID present in /models AND has publicly published Hugging Face weights, /models dump committed as eligibility evidence) is **not recorded as done**; treat it as open, with the fallback bench in `refinement/02` ready if an ID fails. Do not re-plan work the file genuinely shows as done; do surface its gaps (embedding model unpinned, pinned-model open-weight verification incomplete, Fetch agents unregistered, Jira role account IDs missing and JSM tier unconfirmed, no GitHub token).
4. `CREDENTIALS-CHECKLIST.md` — the security rules (open-weight-only runtime, no secrets committed, what never to put in the pipeline).
5. Track ground truth: `Hackathon Information/HACKATHON-GUIDE.md`, `conduct-ai.txt`, `fetch-ai.txt`, `fetch-ai-general.txt`, `based-ai-announcement.txt`, `based-ai-general.txt`. Where a judge or verifier needs current detail beyond these exports, fetch the live sources: the DoraHacks bounty pages (1370 Conduct, 1367 Fetch, 1364 BasedAI), the Fetch Innovation Lab docs/hackpack (`innovationlab.fetch.ai`), and the BasedAI submission repo (`github.com/BasedAICo/hackathons` — template, README requirements, rules).
6. `Research/README.md` and `Research/00-verified-claims.md` — the adversarially verified stats every number must trace to (respect the caveat labels: vendor-claimed, vintage corrections, NeuBird sponsorship).

## Hard constraints

- **Locked decisions — do not relitigate:** name = Precedent; full media-vertical demo (MediaCo simulated services carrying real public data); live Jira Service Management with cached fallback; Fetch.ai track committed with gates front-loaded and non-cuttable; BasedAI-aligned permission-aware memory (sponsor-endorsed A/B/C lineage semantics); open-weight-only runtime via Venice; headline stat $600B/yr (Splunk 2026) with the verified $200M/company backup; Disney+ named in the origin story (never assert "Disney runs WHATS'ON" as vendor fact); L3 is called **"Standing Approval"**, never "Autonomous". If one of these fatally conflicts with a track rule you discover, flag it loudly with the evidence and the minimal fix — do not silently change it.
- **The clock and capacity model.** Three deadlines: (1) the Demo Day sign-up form closes **18:00 today, Friday 3 July** — only a human can submit it; assume the team is handling it in parallel, never schedule it as your own task, and treat Demo Day stage time as *probable, not guaranteed* (selected presenters are announced ~22:00 tonight; non-presenters still submit and are still judged on DoraHacks). (2) The BasedAI PR into BasedAICo/hackathons must be final before Demo Day judging Saturday. (3) DoraHacks submission closes **Saturday 4 July 23:59**. Capacity: roughly 24–30 *wall-clock* hours remain between this session and the Saturday code freeze; within them, team capacity is approximately **60–80 productive person-hours** (two near-full-time technical builders, one bounded technical contributor, two non-technical members on non-code work). Cost every proposal in person-hours against this model; it supersedes the older "~30–36h" figure in `00-panel-verdicts.md`. The team starts building **the moment this session ends — Friday, not Saturday** — so the refinement specs must stay build-ready throughout.
- **Cumulative hour ledger.** Additions are constrained cumulatively, not just individually: the existing build plan plus every adopted improvement must fit the capacity model. Any adopted addition names what it displaces (use the idea's cut-lines) or which explicit slack it consumes, and no adopted change may endanger the vertical-slice gate (core demo loop end-to-end on real data). Improvements that don't fit are either marked pitch-narrative-only or rejected. Report the ledger's closing balance in your final message.
- **Honesty standard:** every statistic carries its source and vintage; vendor-claimed numbers stay labelled; nothing invented for the demo may be presented as real; the open-weight-only rule is absolute for the submitted pipeline (Claude builds it; Claude must not run in it).

## The multi-agent system — requirements

Design the orchestration yourself, but it must include these roles, and each judge must ground itself in the **actual published criteria** (from the files and live pages above), not its general instincts:

- **Conduct judge (most important — weight its verdict accordingly).** Scores against the published rubric verbatim: Technical execution 35% ("works reliably on messy and ambiguous inputs, recovers from errors, not a wrapper around a prompt"), Impact & speed-up 30%, User stays in control 20% ("a fully autonomous agent is a non-starter; control beats autonomy"), The demo 20% ("a clear, live before/after a non-engineer can grasp in 90 seconds"). Also enforces their guidance: real process, realistic public data ("the bigger and messier, the better"), credible scale story.
- **Fetch.ai judge.** Published weights (Functionality 25 / Fetch integration 25 / Innovation 20 / Impact 20 / UX 10) and the hard gates as pass/fail: Agentverse registration, Agent Chat Protocol, discoverable & demonstrable inside an ASI:One conversation, primary workflow completes without a custom frontend, public repo. Checks all published bonus levers: multi-agent collaboration, Payment Protocol (FET/Skyfire) monetization, reliability/error handling, creative real-time data use (note: TVmaze in our data plan is a live feed — a nearly free claim), and agents that keep running post-hackathon.
- **BasedAI judge.** The five requirements (ACL sync under concurrent updates; deterministic retrieval-layer enforcement with no LLM in the decision; regulatory-grade audit logs; sub-200ms P99 permission checks; lineage-governed derived memory with revocation propagation), the **open-weight-only rule as a hard eligibility gate**, the separate PR-based submission into BasedAICo/hackathons with its README requirements, the sponsor's Discord-endorsed A/B/C derived-artifact semantics, and the two published bonus challenges (temporal access rules; query-time inference prevention) — the temporal-embargo rule is currently cut for scope, so weigh whether a cheap partial or Q&A-narrative claim is worth adopting.
- **Technical-execution judge.** Build feasibility against the capacity model, live-demo failure modes, whether the architecture claims are real engineering; brutally concrete fixes only. This judge also runs an **architecture-completeness audit**: `refinement/02` must leave no design decision for the builders to improvise mid-build. Verify it pins, in writing: the final tech-stack table (language, frameworks, storage, model IDs incl. the embedding model, UI stack for the console); the repo/module layout; the complete data schemas; the agent-to-agent message contracts AND the backend↔console API contract; the demo-day runtime topology (what runs where, ports, startup order, fallback modes); and sequence flows for the three demo beats end-to-end. Every gap found becomes a builder task in this session — the mission is not done while the blueprint has holes.
- **Demo-Day VC judge.** 3-minute pitch quality, memorability, market/moat credibility, the ask, and the uncomfortable Q&A.
- **Builder agents** that do further research, thinking and brainstorming to *improve* the idea — not just patch weaknesses. Give them licence to propose additions that raise ceiling scores (a sharper wow moment, a stronger measured artifact, a better 90-second before/after), each costed in person-hours against the ledger.
- **Adversarial verification** of any new factual claim before it enters a deliverable, and **live-source verification** of every eligibility-relevant assumption (bounty rules, Fetch gate wording, BasedAI PR requirements) rather than trusting the local exports alone. If a live source is unreachable or unparsable after two attempts, fall back to the local export, record the item in the working notes and final report as "LIVE CHECK FAILED — verified against local export only", and continue; never present an assumption as live-verified without a page actually fetched in this session.
- **Convergence loop with anti-inflation rules.** Re-judge after each build round using **fresh judge instances** that commit their scores before seeing prior rounds' numbers. Judges score the entry **as it will exist at the submission deadline given the hour ledger** — unbuilt claims score as absent, not as planned — and every criterion score must cite concrete evidence; each judge closes with the single question a real judge is most likely to ask that we cannot yet answer. Stop when no fatal or major findings remain AND a round yields marginal movement, with a hard cap you choose. If the cap is reached (or two consecutive rounds move no score by more than ~2 points) while fatal or major findings remain, **stop and do not adjust scores to force convergence** — report the residual findings ranked, with the honest reason each cannot be closed inside the window. A round that raises scores without new built-or-verified substance is a warning sign, not progress.

## Deliverables (all in the repo, updated in place)

1. `Idea/Idea-Development.md` advanced to **v5** — the definitive idea document, incorporating everything adopted.
2. The five `Idea/refinement/` specs updated wherever your rounds changed them (data plan, architecture, deck, demo/video scripts, scale story & Q&A) — the team builds from them starting Friday, so they must stay build-ready and internally consistent (naming, numbers, demo beats identical across all files). `refinement/02` specifically must exit this session as the **definitive architecture blueprint** per the completeness audit above: exact tech stack, systems design, schemas, contracts, topology and sequence flows — sufficient for the two lead builders to code without making a single unplanned design decision.
3. `Idea/refinement/00-panel-verdicts.md` appended with this session's round history: scores per judge per round, what changed, what remains.
4. A final message that leads with the outcome: final scores vs. the 79/80/80/80/79 baseline, the ranked list of changes you adopted, anything you rejected and why (one line each), the hour-ledger balance, remaining risks (including any LIVE CHECK FAILED items), and any genuine decision points that belong to the humans — clearly separated from what you already decided.

## Operating rules

You are operating autonomously; nobody will answer questions mid-run. For anything reversible that serves the mission, proceed. When you have enough information to act, act — do not re-derive what the repo already establishes or narrate options you will not pursue. Before reporting progress or scores, audit each claim against something concrete from this session (a file you wrote, a page you fetched, a scorecard an agent returned); if something is unverified, say so explicitly. Keep a working-notes file under `Idea/refinement/` recording decisions and their reasons as you go, so the session is auditable afterwards. Your final message is for teammates who did not watch you work: lead with what happened, write complete sentences, no invented shorthand.

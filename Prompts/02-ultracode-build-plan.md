ultracode

# Mission: the build plan — from refined idea to submitted, rehearsed, winning entry

(Paste this into the **same session** after the refinement mission completes, so you keep full context of the v5 idea and every deliverable spec.)

The idea is now settled to the extent your refinement round settled it; where your final report left genuine decision points to the humans, plan both branches or the safe default and say which you assumed. Produce the **complete execution plan** that takes Precedent from the current repo state to a submitted DoraHacks entry, a merged-ready BasedAI PR, live Fetch.ai artifacts, a finished deck and video, and a fully prepared five-person team — by Saturday 4 July 23:59. Be ambitious: plan to win the Conduct track, not merely to submit — but every hour you schedule must be real, and the plan must survive its own red-team. Use a multi-agent pass to build it (parallel workstream planners, then a feasibility judge and a schedule red-team adversarially checking dependencies, hour-math and single-points-of-failure) before you finalize.

**Anchor the timeline to the actual wall-clock time when you write this plan — run `date`; do not assume Friday morning.** Where a previously mandated gate time is no longer achievable from that anchor (vertical slice Friday 18:00, Jira first-hour verification, Friday-night BasedAI draft PR), keep the locked gate ORDER (vertical slice → code freeze + video recording → BasedAI draft PR → rehearsal go/no-go), re-set the times explicitly, mark them as re-timed, and state what got compressed to compensate — never pretend.

## The team you are planning for

Five people. Plan by these profiles (the humans will map names to profiles themselves):

- **T1 and T2 — technical.** Each has a **Claude Max (20×) subscription** and a **ChatGPT Pro subscription (with the hackathon's Codex boost, valid to 7 July)**. The bulk of the programming happens on these two seats through Claude Code. Plan them as the parallel engines of the build: independent workstreams in separate git worktrees so they never block each other; long agentic Claude Code sessions with the full task spec given up front; explicit guidance on when to lean on subagents/parallel agents versus working directly. Usage replenishes in rolling ~5-hour windows AND is subject to weekly caps — these planning sessions have already consumed part of one Max seat's weekly budget, so name which seat ran them and bias that person's heaviest load away from it. Stagger T1's and T2's most intensive blocks rather than running both flat-out simultaneously; include a twice-daily usage-check ritual with a pre-decided overflow order (ChatGPT Pro/Codex for second-opinion code review, throwaway scripts, and spillover work when Claude windows bite).
- **T3 — technical, Claude Pro (meaningfully lower limits than Max).** Scope T3 to bounded, high-value work that doesn't need marathon sessions — code review of T1/T2's output, the P99 benchmark harness, seeded-data QA, the Jira permission-scheme setup and verification, integration testing of the Fetch gates. Give T3 tasks with crisp boundaries so a limit interruption never blocks the critical path.
- **N1 and N2 — non-technical.** They are not spare capacity; they are load-bearing, and the plan must **teach itself to leverage them**. For every N-task, the plan includes a ready-to-use Claude workflow packet they can run in the Claude app/claude.ai without touching code: the exact prompt to paste, the files to attach, what "done" looks like, and the hand-back path. Each packet must be executable by someone with **no repo access**: name who bundles and sends them the packet plus attachment files (and via what channel), and who receives their output and commits it where (e.g. T3). State the claude.ai plan you are assuming for N1/N2 in the plan's assumptions box; if it might be free-tier, size every packet to fit (chunked prompts, few small attachments, no single marathon generation). Their natural territory: producing the pitch deck from the deck spec; the demo video's script, shot list, captions and edit decisions; DoraHacks BUIDL admin and Demo Day logistics; drafting the BasedAI PR README from the architecture doc; adapting the KB/runbook content from the data plan; verifying data-source licences and attribution lines; the practitioner-outreach task the VC judge demanded (with a Claude-drafted outreach script); play-testing the demo as a naive user and filing real Jira tickets against it; running pitch rehearsals with a Claude-generated question bank and timing the 3-minute cut. Design at least this much — and improve on it.

**Availability envelope (plan against this; the team corrects numbers in one place, not you):** assume T1/T2/T3 are each available ~12 hours Friday (from when this plan lands) and ~5 hours Saturday outside Demo Day; N1/N2 ~8 hours Friday and ~4 hours Saturday. All five attend Demo Day in person (Blackett LT1, 10:00–16:30 Saturday); those hours plus ~1 hour travel/setup are NOT build hours for anyone on site. State these assumptions in a visible box at the top of BUILD-PLAN.md, and have the red-team recompute the fit if any number changes.

## Plan requirements

1. **Workstreams, dependencies, and a timeline with gates.** Decompose everything that remains. Consult `CLAUDE-AVAILABLE-APIS.md` for what is already provisioned — do not re-plan finished work, but schedule its listed gaps PLUS every unresolved fatal/major item in `Idea/refinement/00-panel-verdicts.md` (in particular: the Venice open-weight ID verification with committed /models evidence — the APIs file verifies API access only, not the pinned IDs' eligibility; and the Jira JSM-tier + permission-scheme role-flip verification — the APIs file verifies auth and issue creation only). Show the dependency graph, the critical path, and hard gates with times, including the vertical-slice gate (core demo loop end-to-end on real data) early enough to protect the demo, a code-freeze + video-recording gate, the BasedAI draft-PR gate, and a rehearsal gate with a written go/no-go rule for live-demo vs recorded fallback. Use the same capacity model stated in the refinement mission; show person-hour totals per person per day and the wall-clock gates separately. If the plan doesn't fit, cut scope using the idea's cut-lines rather than pretending.
2. **Per-profile task tracks.** For each of T1/T2/T3/N1/N2: their ordered task list with time estimates, what they need from others and when, and their definition of done. For T1/T2 include the Claude Code session strategy per major task (one well-specified kickoff prompt per session, what context files to reference, verification steps the session must run before claiming done). For N1/N2 include the full workflow packets described above.
3. **Visual elements — deck and video.** Plan their production concretely from `Idea/refinement/03-pitch-deck.md` and `04-demo-and-video-script.md`. The demo video's spine is **screen recordings of the real product** — never fake the product. The asset list must include the raw screen-capture session itself as a task with a **technical owner** and a start time pinned to the code-freeze gate (deliverable: labelled raw clips in a shared location), and must name the assembly tool N1/N2 will actually use (e.g. CapCut/iMovie/Descript), with their Claude packet covering script, shot list, captions and edit decisions — not the cutting itself. For generated visuals (cold-open hook, b-roll, 30-second teaser, title cards): **first check whether the Higgsfield MCP is actually connected to this session** (list your tools) — if it is, validate one test generation before planning around it; if it is not (likely — no Higgsfield account or MCP config exists in this repo yet), plan the clean screen-recording-only cut as the PRIMARY video plan, make generated visuals a strictly optional enhancement track that cannot block the video gate, and add "set up Higgsfield account + connect MCP, or explicitly skip" as a ~15-minute human decision task with an owner and a decision deadline. Treat Venice image generation for stills/diagram art the same way: verify, then plan. Each generated-asset task states its access path — if the tool is only reachable from a Claude Code session, a bounded T-slot runs the generation while N1/N2 own the prompt drafts, shot list and quality review. Every on-screen stat must trace to `Research/00-verified-claims.md` with its caveat labels respected.
4. **Submission logistics as first-class tasks with owners and times.** DoraHacks BUIDL (repo, deck, video, bounty ticks: Conduct + Fetch.ai + BasedAI); the Fetch deliverables (public ASI:One shared-chat URL, Agentverse profile URLs, 3–5 min video); Demo Day (first-hour checklist item: confirm the sign-up form went in before Friday 18:00; treat the ~22:00 selection announcement as a scheduled decision point with a named owner). For the BasedAI fork/PR: **no GitHub token is configured, so repo publication and the fork/PR are human tasks** unless the plan explicitly provisions `GITHUB_TOKEN` per `CREDENTIALS-CHECKLIST.md` — decide which, name the owner and time either way, and if human, put the exact steps in an N-workflow packet (fork, copy `_TEMPLATE` to the team folder, README fields, PR touching only the team folder, no secrets). Making the GitHub repo public (a Fetch hard gate) requires a **git-history secrets scrub first** (.env, tokens, evidence dumps) — schedule it with an owner and time. Pin the BasedAI PR's final-ready time to before Demo Day judging Saturday morning, distinct from the draft-PR gate. Nothing here is allowed to be "someone does it at 23:00 Saturday".
5. **Risk register and contingencies.** For each top risk — live-demo failure, Venice latency mid-pitch, Jira/venue-Wi-Fi failure, usage limits exhausted at the wrong moment, a teammate blocked, generated-visual quality misses, and **not being selected to present at Demo Day** (announcement ~22:00 Friday) — give the trigger, the mitigation already built into the schedule, and the pre-decided fallback. For the selection risk specifically: write the Saturday schedule with an explicit selected/not-selected branch decided at the announcement — if selected, the rehearsal and prep gates stand; if not, redirect the pitch-rehearsal hours to video quality, submission polish and the BasedAI PR, and say exactly which tasks absorb them (DoraHacks/BasedAI/Fetch judging is unaffected either way).
6. **THE FINAL PHASE — presentation prep for all five (schedule real hours; not optional polish).** Split it in two. **(a) Draft now, in this session**, the prep documents that don't depend on the finished build — write them directly into `Prep/`: an industry primer distilled from `Research/` (the ten numbers, the competitive map, the white space, honest caveats); the complete Q&A bank from `refinement/05` extended with judge-specific questions and crisp spoken-style answers; a tech-stack explainer at two depths (engineer-level, and plain-English with analogies for N1/N2); and a glossary of every term a judge might use back at us (KEDB, ITIL standard change, P99, lineage, MTTR…). **(b) Specify in `Plan/prep-spec.md`** the build-dependent documents to be generated after the relevant gates — the systems-design walkthrough with diagrams (final architecture), the demo run-of-show with every click, line and fallback cue (post demo-mode decision), and per-profile crib sheets (what each person owns on stage, their likely questions, their two takeaway lines) — each with who generates it, with which prompt, and when, plus an update-pass prompt for the drafts in (a). Schedule a table-read rehearsal where each person is quizzed from the bank — including a run where a technical question goes to a non-technical member and they handle it with the crib sheet.

## Output

Write the plan to `Plan/BUILD-PLAN.md` (create the folder) with the assumptions box at the top, the N1/N2 workflow packets in `Plan/workflows/` (one file each, self-contained), the build-independent prep drafts in `Prep/`, and the prep-phase specification in `Plan/prep-spec.md`. End with a final message that leads with the outcome: the critical path in plain sentences, the three riskiest moments in the schedule and their pre-decided answers, and the first task each of the five people should start the moment they read the plan.

## Operating rules

Same as before: you are autonomous; act when you have enough information; audit every schedule claim against the actual hour-math and the repo's real state; nothing in the plan may depend on work the plan itself doesn't schedule. The deadline math is unforgiving — where ambition and the clock collide, the demo and the Conduct rubric win, and you say so explicitly rather than hiding the trade-off.

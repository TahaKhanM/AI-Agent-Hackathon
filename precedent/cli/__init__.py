"""Precedent command-line surface (WP-POLICY authoring kit).

Authoring the policy pack is CLI-only — there is no in-console pack editor (the console
executes the deterministic gate; it never authors it). The `precedent policy lint` command
is the guard an author runs before shipping a pack.
"""

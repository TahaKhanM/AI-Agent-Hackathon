# Human action items — T1 & T3 build (the things the autonomous prompts cannot do)

> The two Ultracode build prompts — [`Prompts/03-ultracode-t1-completion.md`](../Prompts/03-ultracode-t1-completion.md) (T1) and [`Prompts/04-ultracode-t3-completion.md`](../Prompts/04-ultracode-t3-completion.md) (T3) — run autonomously and do **everything an agent can do**: all code, tests, the bench, the KB corpus, the seed data, the submission *drafts*, the scrub *commands*. This file is the complement: **the account-level / on-stage / judgement acts a human must perform**, because no GitHub token, Agentverse session, or Jira/ASI:One login is wired into the repo by design. The agents **prepare each of these to a single click / paste** and write the exact command or file; you execute.
>
> This is the concrete, gated, deduped version of [`BUILD-PLAN.md §1.1`](BUILD-PLAN.md). Tick as you go. Ordered by the gate (G0 → G6) each item is needed by. Track tags: **[T1]**, **[T3]**, **[both]**.

---

## G0 — Kickoff (Friday start, before the two build sessions run hard)

- [ ] **[both] Ratify the canonical seed `4207`.** Confirm `4207` (or agree an override) as the single shared constant that binds T3's mutation corpus to T1's incident generator/fixtures. *Why:* one seed → one number across chip / slide 10 / README / BUIDL; both prompts default to `4207` — if you override, change it in **both** sessions together. It does not exist in code yet; the agents create it. *(One-word confirm.)*
- [ ] **[T3] Create the T3 worktree** off `build/t3-bench-submissions` and merge the **latest `main`** into it (main is advancing as T1 lands commits). *Why:* the bench must run against the real, current product; the branch is behind main. *(Agent supplies the exact `git worktree add` + merge commands.)*
- [ ] **[T3] Open the BasedAI skeleton PR** from your GitHub account. Fork `github.com/BasedAICo/hackathons` → clone to `~/basedai-hackathons` → `git checkout -b precedent-submission` → check a merged/open PR (e.g. #3 "BioVault") for the exact submissions-folder path → `cp -r UK-AI-Agent-EP5/submissions/_TEMPLATE .../submissions/<team-name>/` → paste the **agent-drafted README** (8 verbatim headings, open-weight declaration, six attack names, `docs/compliance` cites, placeholder video link) → paste the **agent-drafted Venice-only `.env.example`** (template's ANTHROPIC/OPENAI keys deleted) → commit, push, open PR titled **"Precedent — permission-aware agent memory (UK AI Agent Hackathon EP5)"** touching **only** your team folder. *Why:* no GitHub token is wired; the PR is the **only** channel into the BasedAI track, and a skeleton-at-G0 satisfies both deadline readings.
- [ ] **[T3] Ask a BasedAI mentor which deadline governs** (event README "3 Jul EOD" vs track doc "4 Jul before judging"). Record the answer in the PR (replace the `[[WAIT:MENTOR-ANSWER]]` sentinel). *Why:* sets when numbers must be committed; only a human can ask.
- [ ] **[both] Register the three Fetch agents on Agentverse** (Watcher / Librarian / Operator) as **mailbox** agents, using the **stable env seeds the agents wrote** (so the address survives T1's handler swap). Confirm both badges render (`innovationlab` 3D8BD3 + `hackathon` 5F43F1); capture the 3 profile URLs; fill the empty `.env` IDs (seeds, mailbox key, profile URLs). *Why:* account creation can't run headless; **both build prompts defer this to you**. The Fetch integration is a **pass/fail hard gate — partial scores zero** — and registering early starts the ≥10-interaction discoverability clock.
- [ ] **[both] Run ≥10 discoverability chats + capture an *insurance* ASI:One shared-chat URL** early (the final anchored one comes at G4). Fill `ASI_ONE_SHARED_CHAT_URL`. *Why:* spends ASI:One quota and needs a human session; feeds the DoraHacks Fetch click-block and the deck; the insurance URL protects against a venue-network failure at demo time.
- [ ] **[both] *(optional)* Invite the 2nd Jira seat.** ~30 min: invite a 2nd Jira user, add to role `10007`, fill `JIRA_RIGHTS_OPS_ACCOUNT_ID` / `JIRA_SCHEDULING_OPS_ACCOUNT_ID` in your **local** `.env` (never committed). *Why:* makes incident 3's deny/allow split two named principals (a cleaner segregation-of-duties story). **Fallback:** single-account role-flip between `10007`/`10008` — the prompts already handle this. Not on the critical path.

---

## G1 → G2 — Friday build to the code-freeze / capture (leave a solid evening margin)

- [ ] **[both] Coordinate the seed-`4207` mutation-corpus hand-off to T1** (~18:30, after the vertical-slice gate). The T3 agent produces the byte-identical 100-mutation corpus + a loader; make sure T1's session runs the extractor bench over it and records the single robustness number for the chip / slide 10 / README / BUIDL. *Why:* cross-lane coordination; the same number must appear on all four surfaces.
- [ ] **[T3] Run the five-pass secrets scrub (A–E)** on the main repo (gitleaks `--all --redact`; `.env`-never-committed check; credential-family pattern grep; literal-value scan; committed-dump sweep — exact commands pre-filled into the checklist by the agent, from [`T3-github-publication.md §3`](workflows/T3-github-publication.md)). Eyeball each surviving hit and **make the leak call yourself**. *If a real leak:* rotate the key on the vendor dashboard **first**, then `git-filter-repo` only with T1's OK. *Why:* an agent can interpret redacted output but a human decides; any single pass is insufficient; this is a **hard predecessor** to the repo going public.
- [ ] **[T3] Flip the main repo PUBLIC** (after the scrub is clean). Pre-flight: scrub clean; README has run instructions; Fetch badges on each agent README section; LICENSE + data-attribution lines present. GitHub → Settings → Danger Zone → Change visibility → Make public. Verify logged-out in an **incognito** window (README + `docs/compliance/` + `data/analysis/` render). Write the scrub-evidence line into `docs/evidence/README.md`. *Why:* a private repo **fails the Fetch hard gate**, and this **unblocks N2's DoraHacks draft and N1's deck**.
- [ ] **[T3] Announce each gate + the URLs** in the team thread (public repo URL, PR URL, `precedent_memory/bench/RESULTS.md` path). *Why:* N1's deck and N2's DoraHacks packet are blocked waiting on these.
- [ ] **[T3] Commit the measured bench numbers into the BasedAI PR before Fri 21:00.** Paste the agent's measured 10-metric table + mutation line + 6/6 attack results (replace the `[[WAIT:BENCH-SYNTH]]` / `[[WAIT:ATTACKS]]` / `[[WAIT:BENCH-CURVE]]` sentinels); state oracle-independence explicitly; re-check "Files changed" lists **only** your team folder. *Why:* the PR must never be numbers-free under the strict deadline reading.
- [ ] **[T3] Freeze the DoraHacks one-shot organizer answers with T1 sign-off.** Open the BUIDL form (event **2272**) **without submitting**; copy every organizer question verbatim into `docs/evidence/` + screenshot; the agent drafted answers; get T1 sign-off on **every** answer; resolve every `[NEEDS-FACT]` before review. *Why:* **organizer answers lock permanently at draft-submit and can never be edited** — improvising at the form is unrecoverable.

---

## G3 — After the repo is public (~evening Friday)

- [ ] **[T3] Submit the DoraHacks BUIDL draft** (event **2272**). Enter the signed answers character-for-character; tick **exactly three** bounties **1370 / 1367 / 1364** (deselect any pre-selected extras); run the incognito link-check + a no-`‹` / no-`XX` Ctrl-F; submit; screenshot the submitted state and post to the thread. *Why:* the BUIDL enters Conduct + Fetch (the PR is what enters BasedAI); irrelevant ticks dilute the story; needs the shared login and a human at the form.

---

## G4 → G6 — Saturday: realism run, finalise, submit

- [ ] **[T3] Run the UCI 25k realism run + live drift/TTC** (in front of judges if presenting). Caption **"25k-record store"**, **never "141k events"**. The live Jira flips (roles `10007`/`10008` + issue-security `10000`/`10001`) read each flip timestamp from `/rest/api/3/auditing/record`. Needs live Jira creds in your local `.env`. Paste values as a PR comment. *Why:* live creds + on-stage execution need a human; this is the claim a synthetic-only competitor structurally cannot make. *(Agent built the commands; they exit non-zero when unconfigured.)*
- [ ] **[T3] Push the BasedAI PR final** before Saturday judging: paste the demo video URL (from N2's unlisted upload, replacing `[[WAIT:VIDEO-LINK]]`); add any late realism number; **re-run gitleaks on the fork** before the final push; rebase onto `upstream/main` if `BasedAICo/hackathons` moved; verify only-your-folder; comment "Final for judging — video + measured benchmarks included." *Why:* the fork carries your commits too and must be re-scrubbed; final-ready is the judging gate; the push is account-level.
- [ ] **[T3] Final DoraHacks submit before 23:59 BST / 22:59 UTC** (never plan on the last hour). Paste the video URL + any late number; re-run the pre-submit checklist; screenshot; commit the final page text + locked one-shot answers to `docs/evidence/dorahacks-buidl.md`. *Why:* missing 23:59 BST loses the entry.
- [ ] **[both] Watch for the Demo-Day presenter announcement** (~22:00 Fri onward, Discord/email). If **not** selected to present, redirect to the no-stage surfaces (90-second cut first on the BUIDL page, RESTRICT/party-trick video inserts). T1 owns the branch decision; post the result in the thread. *Why:* only a human can watch for and react to the organizer announcement.

---

### Notes
- **Everything not on this list is automated** by the two build prompts — code, tests, the bench + oracle + 6/6 attacks, the KB corpus + seed data, and every submission *draft* (PR README, `.env.example`, DoraHacks worksheet, scrub checklist).
- **Downstream lanes (N1/N2) wait on the T3 items here** — chiefly the public-repo URL (unblocks the DoraHacks draft and the deck) and `RESULTS.md` (fills the deck's slide 10 and the PR/video number cells).
- Keep every secret in your **local `.env`** — the scrub gate (and public repo) fail on a single committed key, token, accountId, or real teammate name.
